import { AxiosInstance } from "axios"

export const getSubmittedPreApprovalsCoord = async (axios: AxiosInstance, coordinatorId: string) => {
    // const response = await axios.get<ResponsePreApprovalFormList>(`/coordinator/submittedLearningAgreements/${coordinatorId}`)
    // return response.data
}